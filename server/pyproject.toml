[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rn727", email = "227737184+rn727@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[server]>=0.141.1",
]

[project.scripts]
server = "server:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
